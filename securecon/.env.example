# Copy this to .env.local and fill in your values
# NEVER commit .env.local to version control

# Required: your Anthropic API key (server-side only, never exposed to client)
ANTHROPIC_API_KEY=sk-ant-your-key-here
